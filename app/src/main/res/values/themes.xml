<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Weather_application" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- colorPrimary - основные цвета приложения -->
        <item name="colorPrimary">@color/status_bar_color</item>
        <!-- цвет статусбара -->
        <item name="android:statusBarColor">@color/status_bar_color</item>
    </style>

    <style name="Theme.Weather_application" parent="Base.Theme.Weather_application" />
</resources>